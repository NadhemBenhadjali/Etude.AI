apiVersion: v1
kind: Secret
metadata:
  name: etude-ai-secrets
  namespace: etude-ai
type: Opaque
stringData:
  # Neo4j - Replace with your cloud Neo4j credentials
  neo4j-password: "YOUR_NEO4J_PASSWORD"
  neo4j-user: "neo4j"

  # PostgreSQL - Replace with your database credentials
  postgres-password: "YOUR_POSTGRES_PASSWORD"
  postgres-user: "admin"
  postgres-db: "Etudeai"

  # Redis (usually no password for development)
  redis-password: ""

  # Keycloak - Replace with your Keycloak admin credentials
  keycloak-admin: "admin"
  keycloak-admin-password: "YOUR_KEYCLOAK_PASSWORD"
  keycloak-realm: "etudeai"
  keycloak-resource: "etudeai-backend"

  # LLM API - Replace with your API key
  llm-api-key: "YOUR_LLM_API_KEY"
  llm-model: "mistral/mistral-large-latest"
  embedding-model: "mistral/mistral-embed"

  # Qdrant - Replace with your Qdrant API key
  qdrant-api-key: "YOUR_QDRANT_API_KEY"

  # ElevenLabs TTS - Replace with your API key
  elevenlabs-api-key: "YOUR_ELEVENLABS_API_KEY"

  # Ngrok (optional) - Replace with your auth token
  ngrok-auth-token: "YOUR_NGROK_AUTH_TOKEN"

  # Sentry - Replace with your Sentry DSNs
  sentry-dsn: "https://YOUR_PYTHON_SENTRY_DSN@sentry.io/project"
  sentry-dsn-backend: "https://YOUR_JAVA_SENTRY_DSN@sentry.io/project"

  # Environment
  environment: "production"
  release-version: "v1.0.0"

---
# ConfigMap for non-sensitive configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: etude-ai-config
  namespace: etude-ai
data:
  # Application URLs
  AI_PIPELINE_URL: "http://ai-pipeline:8000"
  TTS_SERVICE_URL: "http://tts-service:8000"

  # Keycloak
  KEYCLOAK_REALM: "etudeai"
  KEYCLOAK_RESOURCE: "etudeai-backend"
  KEYCLOAK_PUBLIC_URL: "http://localhost:8083"
  KEYCLOAK_AUTH_SERVER_URL: "http://keycloak:8080"

  # Database hosts - REPLACE WITH YOUR CLOUD URLS
  NEO4J_URI: "neo4j+s://YOUR_NEO4J_CLOUD_URI"
  POSTGRES_HOST: "postgres"
  POSTGRES_PORT: "5432"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  REDIS_URL: "redis://redis:6379/0"
  QDRANT_URL: "https://YOUR_QDRANT_CLOUD_URL:6333"

  # AI Models
  LLM_MODEL: "mistral/mistral-large-latest"
  EMBEDDING_MODEL: "mistral/mistral-embed"

  # CORS - Update with your allowed origins
  ALLOWED_ORIGINS: "http://localhost:4200,http://localhost:8081"

  # Environment
  APP_ENV: "production"

